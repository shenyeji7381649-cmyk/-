# 贡献指南（Contributing）

本文档用于统一 React + Node.js + PostgreSQL 项目的协作方式。

## 1. 分支与提交

- 从主分支切出功能分支：
  - `feat/<short-name>`：新功能
  - `fix/<short-name>`：问题修复
  - `chore/<short-name>`：工程维护
- 推荐提交信息（Conventional Commits）：
  - `feat: ...`
  - `fix: ...`
  - `docs: ...`
  - `refactor: ...`

## 2. 开发基本要求

提交 PR 前建议至少满足：

1. 前后端都能在本地正常启动
2. 新增或修改逻辑有相应测试
3. lint / test / build 通过
4. 涉及数据库结构变更时，提供 migration

## 3. PR 模板（建议）

### 背景
- 为什么做这次改动？

### 改动内容
- 具体改了什么？

### 风险与影响
- 是否影响接口兼容性？
- 是否涉及数据库 schema 变更？
- 是否需要灰度或回滚方案？

### 验证方式
- 本地执行了哪些命令？
- 核心场景如何验证？

## 4. 代码评审关注点

- 命名是否清晰、模块边界是否合理
- 是否有重复逻辑可下沉复用
- 是否处理了异常、超时、空数据等边界
- SQL 是否考虑索引与性能
- 是否存在敏感信息泄露风险

## 5. 数据库变更约定

- 禁止手工直接改线上库结构
- 所有 schema 变更通过 migration 管理
- 迁移脚本必须可重复执行或可回滚（视框架约定）
- 大表变更需评估锁表风险与执行窗口
